language: c
compiler:
  - clang
  - gcc

os:
  - linux
dist: xenial

before_install:
  - sudo apt-get -y install flex bison pkg-config libreadline6 

script: 
  - make

notifications:
  email: false
branches:
  only:
    - master

